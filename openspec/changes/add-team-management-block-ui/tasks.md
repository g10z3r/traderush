## 1. Доменная модель и общий слой команд

- [x] 1.1 Проверить текущее поведение `TeamService`, `Team` и `InMemoryTeamRepository`, чтобы определить минимальный API, нужный для операций UI
- [x] 1.2 Добавить доменную поддержку переименования команды без изменения её id, очков или участников
- [x] 1.3 Убедиться, что валидация переименования переиспользует те же правила trim, длины и уникальности имени, что и создание команды
- [x] 1.4 Убедиться, что UI-facing правила rename/delete отклоняют непустые команды без force deletion
- [x] 1.5 Добавить или обновить общий mapping ошибок/сообщений команд, чтобы UI и чат-команды могли единообразно показывать понятные результаты
- [x] 1.6 Добавить или обновить focused tests для валидного переименования, duplicate/invalid rename, отклонения переименования непустой команды и существующего поведения create/join/leave/delete

## 2. Регистрация блока и серверная интеграция

- [x] 2.1 Добавить `TeamManagementBlock` с server-side обработкой взаимодействия игрока
- [x] 2.2 Добавить регистрацию блока и предмета блока в пространстве имён Trade Rush
- [x] 2.3 Зарегистрировать блок/предмет управления командами во время `TradeRush.onInitialize()`
- [x] 2.4 Добавить server-opened menu/factory path, чтобы взаимодействие с блоком открывало UI управления командами
- [x] 2.5 Сохранить независимость блока от поведения и naming vanilla command block

## 3. Снимки состояния команд и networking

- [x] 3.1 Определить serializable snapshot data для строк команд, выбранной/текущей команды, очков, количества участников и display names или стабильных идентификаторов участников
- [x] 3.2 Определить client-to-server payloads для действий create, join, leave, delete empty, rename empty и refresh
- [x] 3.3 Определить server-to-client payloads для обновлённых snapshots и ошибок операций
- [x] 3.4 Зарегистрировать server-side payload handlers, которые валидируют контекст игрока и вызывают `TeamService`
- [x] 3.5 Обновлять snapshot UI запрашивающего игрока после каждого успешного действия, изменяющего команды
- [x] 3.6 Возвращать понятную ошибку без изменения состояния, когда валидация не проходит или целевая команда отсутствует

## 4. Клиентский UI

- [x] 4.1 Зарегистрировать screen/menu управления командами в `TradeRushClient`
- [x] 4.2 Реализовать минимальный screen управления командами со списком команд, деталями выбранной команды, статусом текущей команды игрока и кнопками действий
- [x] 4.3 Добавить flow текстового ввода для создания команды
- [x] 4.4 Добавить flow текстового ввода для переименования выбранной пустой команды
- [x] 4.5 Отключать или отклонять UI actions, которые невалидны для текущего выбора, например удаление или переименование непустых команд
- [x] 4.6 Отображать обновлённое состояние команд и ошибки операций, возвращённые сервером

## 5. Assets, локализация и документация

- [x] 5.1 Добавить минимальные blockstate, block model, item model и loot table assets для блока управления командами
- [x] 5.2 Добавить localization strings для имени блока, заголовка UI, кнопок, labels полей и сообщений об ошибках/результатах
- [x] 5.3 Задокументировать block-based flow управления командами в README или другом проектном документе
- [x] 5.4 Подтвердить, что существующие команды `/traderush team` и `/tr team` остаются задокументированными как резервные/admin/debug операции

## 6. Проверка

- [x] 6.1 Запустить сборку проекта через `./gradlew build`
- [x] 6.2 Вручную проверить в dev world, что блок можно поставить и что он открывает UI для игрока
- [x] 6.3 Вручную проверить flows create, join, перехода между командами, leave, delete empty, reject delete non-empty, rename empty и reject rename non-empty
- [x] 6.4 Вручную проверить, что состояние команд сохраняется после перезапуска сервера после операций через UI
- [x] 6.5 Вручную проверить, что существующие чат-команды управления командами всё ещё работают после добавления UI

## 7. Переработка UI layout после review

- [x] 7.1 Переработать screen управления командами на трёхколоночную структуру: список команд, детали выбранной команды, действия/создание команды
- [x] 7.2 Сделать список команд вертикально скроллируемым при переполнении первой колонки
- [x] 7.3 Добавить независимые визуальные выделения текущей команды игрока и выбранной для просмотра команды, включая случай совпадения этих состояний
- [x] 7.4 Реализовать initial selection: текущая команда игрока, иначе первая команда в списке, иначе отсутствие выбранной команды без ошибки
- [x] 7.5 Переработать вторую колонку: количество участников, очки и скроллируемый список участников выбранной команды
- [x] 7.6 Добавить empty state во второй колонке, когда команда для просмотра не выбрана
- [x] 7.7 Переработать верх третьей колонки под действия выбранной команды: join/leave, rename и delete
- [x] 7.8 Показывать Leave только когда выбранная команда является текущей командой игрока; иначе показывать Join для другой выбранной команды
- [x] 7.9 Оставить форму создания команды всегда доступной в нижней части третьей колонки независимо от выбора команды
- [x] 7.10 Сделать Rename и Delete видимыми, но disabled для непустых команд, с grouped explanation о том, что переименование и удаление доступны только для пустых команд
- [x] 7.11 Проверить обычный оконный запуск Minecraft с дефолтным GUI scale: три колонки, scroll areas и основные действия не перекрываются и остаются доступными без необходимости уменьшать GUI scale
- [x] 7.12 Сделать строки списка команд компактными: показывать название команды и опционально количество участников в скобках, без очков и развёрнутых labels
- [x] 7.13 Добавить внутренние отступы в колонки, чтобы текст, списки и controls не прилегали вплотную к границам панелей
- [x] 7.14 Исправить вертикальный layout третьей колонки при дефолтном GUI scale: Join/Leave, Rename, Delete и форма Create Team не должны перекрываться; при нехватке высоты содержимое должно скроллиться или оставаться доступным через другой layout mechanism

## 8. Финальная polish-правка layout и command parity

- [x] 8.1 Вынести кнопку `Refresh` из первой колонки в footer/status row вне трёхколоночной области
- [x] 8.2 Отображать последнее status/result сообщение справа от `Refresh`, сохраняя связь refresh/status как служебной области экрана
- [x] 8.3 Увеличить доступную высоту списка команд за счёт места, которое раньше занимал `Refresh`; список должен доходить до нижнего padding первой колонки
- [x] 8.4 Поднять блок `Create Team` в третьей колонке примерно на 10–15 px и убрать ложный action-scroll, когда содержимое фактически помещается
- [x] 8.5 Добавить чат-команду `/traderush team rename <currentName> <newName>` и alias `/tr team rename <currentName> <newName>`, вызывающие `TeamService.renameTeam`
- [x] 8.6 Реализовать однозначный parsing для rename: `currentName` с support quoted names и suggestions существующих команд, `newName` как greedy string
- [x] 8.7 Обновить README/документацию команд: описать `rename`, ограничение на пустые команды и quoted syntax для текущих имён с пробелами
- [ ] 8.8 Проверить `/traderush team rename` и `/tr team rename` для успешного rename empty team, отсутствующей команды, непустой команды, duplicate/invalid нового имени и имён с пробелами
- [ ] 8.9 Проверить layout в обычном оконном режиме, default GUI scale и меньшем GUI scale: `Refresh`/status не уменьшают список команд, а третья колонка не скроллится без реального переполнения
- [x] 8.10 Запустить `./gradlew build`
